
@.fibonacci_recursive = <int.n>.{
    if n >= 2 {
        // Difficulty with recursion if the type of the function is not yet resolved?
        return fibonacci_recursive(n - 1) + fibonacci_recursive(n - 2);
    } else {
        return 1;
    }
}